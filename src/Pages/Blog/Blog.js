import React from 'react';
import useTitle from '../../hooks/useTitle';

const Blog = () => {
    useTitle('Blog')
    return (
        <div>
            <p className='my-48 text-center'>Coming Soon...</p>
        </div>
    );
};

export default Blog;